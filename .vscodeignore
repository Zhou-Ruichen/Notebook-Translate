# Whitelist: ship only the bundled extension, webview assets, and metadata.
# esbuild bundles src/** into a single out/extension.js, so sources are excluded.
**

!out/extension.js
!media/**
!README.md
!README.en-US.md
!LICENSE
!package.json
!CHANGELOG.md
